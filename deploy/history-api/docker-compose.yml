services:
  history-api:
    image: ghcr.io/nkgotcode/vietmarket-history-api:main
    restart: unless-stopped
    environment:
      PORT: "8787"
      API_KEY: "${API_KEY}"
      PG_URL: "${PG_URL}"
    ports:
      - "127.0.0.1:8787:8787"
